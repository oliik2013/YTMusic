# YTMusic Plasmoid

## WARNING: This thing is very buggy, it works but sucks a bit, but it works with Plasma 6. Not sure that custom port works, sorry.

### Setup:

Install the Plasmoid:

```kpackagetool6 --type Plasma/Applet --install ./plasmoid```

Made by @oliik2013 with help from OpenCode MiniMax M2.5